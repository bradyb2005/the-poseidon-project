Menu Item model test file

There is a functional test that ensures a menu item is initialized properly

An edge case test is there to throw an error if a menu item is missing a restaurant id

Another functional test ensures that if translated into a string, id is included. 
