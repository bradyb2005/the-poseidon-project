test_payment_schema.py: [Feat7-2] This test file verifies that the payment schema correctly handles valid and invalid data. It checks that a PaymentSchema object can be properly created with valid inputs, and that missing required fields or invalid values (such as negative amounts) raise appropriate validation errors. It also ensures that partial updates using UpdatePaymentSchema work as expected without requiring all fields. Additionally, it tests the CostBreakdown model to confirm that cost values are valid and that the total is correctly calculated as the sum of subtotal, delivery fee, service fee, and tax, while invalid totals are rejected.

test_payment_service.py: [Feat6-1] This test file includes tests for subtotal calculation in the PaymentService. It verifies that the subtotal is correctly computed from item prices and quantities, and ensures proper validation by checking that invalid inputs, such as zero quantity or negative prices, raise errors. These additions extend the existing schema tests by covering core business logic for cost calculation.

test_payment_service.py: [Feat6-2] This test file now includes additional tests for fee and tax calculation in the PaymentService. It verifies that delivery fee, service fee, and tax are correctly computed based on the subtotal, including scenarios where delivery fees are waived. It also ensures proper validation by checking that invalid inputs, such as negative subtotals, raise errors. These additions further extend the test coverage to include additional cost components in the overall calculation.

test_payment_service.py: [Feat6-3] This test file now includes additional tests for total cost calculation in the PaymentService. It verifies that the total is correctly computed by combining subtotal, delivery fee, service fee, and tax into a CostBreakdown object, including scenarios where delivery fees are waived. It also ensures proper validation by checking that invalid inputs, such as negative subtotals, raise errors. These additions further extend the test coverage to include complete cost breakdown and aggregation logic.

test_payment_service.py: [Feat7-1] This test file now includes additional tests for retrieving payment information in the PaymentService. It verifies that valid payment data can be extracted and returned in a structured format, including fields such as payment ID, order details, card information, status, and amount. It also ensures proper validation by checking that invalid input, such as a missing payment object, raises errors. These additions further extend the test coverage to include payment information retrieval functionality.

test_payment_service.py: [Feat7-3] This test file now includes additional tests for payment processing in the PaymentService. It verifies that payments are correctly accepted or denied based on card validity using a simulated gateway, and ensures proper validation by checking that invalid inputs, such as a missing payment object, raise errors. These additions extend the test coverage to include payment processing functionality.

test_payment_service.py: [Feat7-4] This test file now includes additional tests for fulfillment request creation in the PaymentService. It verifies that a fulfillment request is successfully created when a payment has been accepted, and that the returned data contains the expected payment and order information. It also ensures proper validation by checking that fulfillment requests cannot be created for denied or missing payment objects. These additions further extend the test coverage to include fulfillment request functionality.

test_payment_router.py: [Feat7-4] This test file now includes comprehensive integration tests for all payment-related API endpoints defined in the PaymentRouter. It verifies successful request handling for subtotal calculation, fee and tax computation, total cost breakdown, payment information retrieval, payment processing, and fulfillment request creation. The tests ensure that valid inputs return the expected responses and status codes, while invalid or malformed requests are properly handled through FastAPI’s validation layer or service-level error handling. Additionally, the file confirms that endpoint responses follow the correct schema structure and that edge cases, such as missing payloads or invalid payment data, are appropriately managed, thereby extending coverage to the full request–response lifecycle of the payment system.
