Test Documentation for menu item model

<img width="848" height="134" alt="Screenshot 2026-03-12 at 9 26 52 AM" src="https://github.com/user-attachments/assets/d4083717-2ac2-4c2f-ad08-0f6fef2ccde2" />

This test runs an initialization test to ensure everything is initialized correctly.
It makes sure all optional fields are none by default

Validation tests:
We have a test done for toggling availability of a menu item.

There are edge case tests for invalid names, zero price, and negative price

Tagging tests:
We have a test that ensures tags are correctly assigned, and another that checks for empty default tags and invalid tags type
